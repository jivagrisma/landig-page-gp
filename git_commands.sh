# Stage all changes
git add .

# Commit the changes with a message
git commit -m "Add testimonials section to landing page"

# Push the changes to GitHub (assuming your remote is named 'origin' and you're on the 'main' branch)
git push origin main
